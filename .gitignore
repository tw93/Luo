__pycache__/
*.pyc
.venv/
venv/
.vercel/
.claude/settings.local.json
CLAUDE.local.md
AGENTS.override.md
.DS_Store
local/

# base fonts (large, not committed)
source/*.ttf
source/*.otf
source/*.woff
source/*.woff2

# generated proof outputs
debug/
print/
proof/*.json
proof/*.txt
proof/*.pdf
proof/*.png
proof/*-preview.html
proof/similarity_*.html
proof/similarity_images/
# proof/gb2312.html is a 7000-line generated coverage page; we no longer
# commit the rendered HTML, vercel regenerates it via vercel.json buildCommand.
proof/gb2312.html
# the public source similarity report is small and useful to commit as a
# baseline gate (regression detection across builds)
!proof/similarity_lxgw.json

# local identity experiments
dist/Luo-Test.*
scripts/compare_similarity.py

# local QA tools that depend on a private reference font; useful on disk
# but not part of the public pipeline
scripts/private_site_study.py
scripts/v04_regression.py

# local cover experiments
assets/images/specimen.svg
